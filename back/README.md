# favourite-movies-mern-app

## Available Scripts

To install back dependencies

### `npm install`

To install front dependencies

### `cd front/`

### `npm install`

To run the entire app (back + front)

### `npm run dev`

Runs the Back-end [http://localhost:5000](http://localhost:5000) & the Front-end [http://localhost:3000](http://localhost:3000)
